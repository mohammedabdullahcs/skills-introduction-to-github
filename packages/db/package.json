{
  "name": "@quran-crm-lms/db",
  "version": "0.0.1",
  "description": "Database layer with Prisma for Quran CRM & LMS",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:seed": "ts-node prisma/seed.ts",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0"
  },
  "devDependencies": {
    "prisma": "^5.6.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}